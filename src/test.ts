import { CustomObj } from './lib/@types/interfaces/CustomArratMethods';

const arr: number[] = [1, 2, 3];
const arrObj: CustomObj[] = [
  { car: 'Dodge' },
  { car: 'Mercedes' },
  { car: 'Mazda' },
];

// const push = arr.myPush(1);
// console.log(push);
// console.log(arrObj);
console.log(arr, arrObj);
